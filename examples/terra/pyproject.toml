[project]
name = "terra-taxi-example"
version = "0.1.0"
description = "Accepted Terra dbt build from the Codex Taxi Distillation Demo"
readme = "README.md"
authors = [
    { name = "Dan Suman" }
]
requires-python = ">=3.12"
dependencies = [
    "dbt-duckdb>=1.9,<2",
    "duckdb>=1.4,<2",
]
